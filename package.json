{
  "name": "workspace",
  "version": "0.0.1",
  "workspaces": [
    "termis_project_api",
    "termis_project_web",
    "lib/*",
    "artifacts/*"
  ],
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0"
  }
}
